@import url('https://fonts.googleapis.com/css2?family=Roboto:wght@400;500&display=swap');
@import url('https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css');

body {
  margin: 0;
  padding: 0;
  background-color: #e0f7fa;
}

.container {
  display: flex;
  justify-content: center;
  align-items: flex-start;
  min-height: 100vh;
  background-color: #e0f7fa;
  padding: 20px;
}

.dash-container {
  width: 100%;
  max-width: 800px;
}

/* Card Styles */
.card {
  background-color: white;
  padding: 15px;
  border-radius: 10px;
  box-shadow: 0 4px 15px rgba(0, 0, 0, 0.05);
  margin-bottom: 15px;
}

.card h3 {
  font-size: 16px;
  font-family: 'Roboto', sans-serif;
  font-weight: 500;
  color: #26a69a;
  margin-bottom: 10px;
}

/* Table Styles */
.history-table-wrapper, .lots-container {
  overflow-x: auto; /* Add horizontal scroll for small screens */
}

.history-table, .lots-table {
  width: 100%;
  border-collapse: collapse;
  border-spacing: 0;
  min-width: 600px; /* Minimum width to ensure scroll works */
}

.history-table th, .lots-table th,
.history-table td, .lots-table td {
  padding: 8px;
  text-align: left;
  font-size: 12px;
  font-family: 'Roboto', sans-serif;
  white-space: nowrap; /* Prevent text wrapping */
}

.history-table th {
  background-color: #e0f7fa;
  color: #333;
  text-transform: uppercase;
  font-weight: 500;
}

.lots-table th {
  background-color: #e0f2f7;
  color: #333;
  text-transform: uppercase;
  font-weight: 500;
}

.history-table tr, .lots-table tr {
  background-color: #ffffff;
}

.history-table tr:nth-child(even), .lots-table tr:nth-child(even) {
  background-color: #f9fbfd;
}

/* Action Button Styles */
.action-btn {
  padding: 6px 12px;
  border: none;
  border-radius: 5px;
  font-size: 12px;
  font-family: 'Roboto', sans-serif;
  font-weight: 500;
  cursor: pointer;
  transition: transform 0.2s ease, background-color 0.2s ease;
  white-space: nowrap;
}

.release-btn {
  background-color: #e57373;
  color: white;
}

.release-btn:hover {
  background-color: #ef5350;
  transform: translateY(-1px);
}

.parked-btn {
  background-color: #81c784;
  color: white;
}

.parked-btn:hover {
  background-color: #66bb6a;
  transform: translateY(-1px);
}

.book-btn {
  background: linear-gradient(90deg, #26a69a, #4dd0e1);
  color: white;
}

.book-btn:hover {
  background: linear-gradient(90deg, #4dd0e1, #26a69a);
  transform: translateY(-1px);
}

/* Search Card Styles */
.search-card {
  padding: 15px;
}

.search-input {
  margin-top: 10px;
}

.search-input .input-field {
  width: 100%;
  padding: 8px;
  border: 1px solid #ddd;
  border-radius: 5px;
  font-size: 12px;
  font-family: 'Roboto', sans-serif;
  background-color: #f9fbfd;
}

.search-input .input-field:focus {
  outline: none;
  border-color: #26a69a;
  box-shadow: 0 0 5px rgba(38, 166, 154, 0.2);
}

/* Enhanced Search Functionality */
.search-container {
  display: flex;
  gap: 10px;
  margin-top: 10px;
}

.search-container .input-field {
  flex: 1;
}

.search-btn {
  padding: 8px 15px;
  background: linear-gradient(90deg, #26a69a, #4dd0e1);
  color: white;
  border: none;
  border-radius: 5px;
  font-size: 12px;
  font-family: 'Roboto', sans-serif;
  font-weight: 500;
  cursor: pointer;
  transition: transform 0.2s ease, background 0.2s ease;
  white-space: nowrap;
}

.search-btn:hover {
  background: linear-gradient(90deg, #4dd0e1, #26a69a);
  transform: translateY(-1px);
}

.search-btn i {
  margin-right: 5px;
}

.lots-container {
  max-height: 400px;
  overflow-y: auto;
  border-radius: 5px;
}

.lots-container::-webkit-scrollbar {
  width: 6px;
}

.lots-container::-webkit-scrollbar-track {
  background: #f1f1f1;
  border-radius: 3px;
}

.lots-container::-webkit-scrollbar-thumb {
  background: #26a69a;
  border-radius: 3px;
}

.lots-container::-webkit-scrollbar-thumb:hover {
  background: #4dd0e1;
}

/* Booking and Release Modal Styles */
.modal {
  display: flex;
  justify-content: center;
  align-items: center;
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background-color: rgba(0, 0, 0, 0.5);
  z-index: 1000;
}

.modal-content {
  position: relative;
  width: 90%;
  max-width: 400px;
  padding: 20px;
  border-radius: 10px;
  animation: slideIn 0.3s ease-out;
}

@keyframes slideIn {
  from {
    transform: translateY(-50px);
    opacity: 0;
  }
  to {
    transform: translateY(0);
    opacity: 1;
  }
}

/* Booking and Release Card Styles */
.form-group {
  margin-bottom: 15px;
  display: flex;
  align-items: center;
}

.form-group label {
  font-size: 14px;
  font-family: 'Roboto', sans-serif;
  font-weight: 500;
  color: #333;
  width: 120px;
  text-align: right;
  margin-right: 15px;
}

.form-input {
  flex: 1;
  padding: 8px 12px;
  border: 2px solid #e0e0e0;
  border-radius: 5px;
  font-size: 14px;
  font-family: 'Roboto', sans-serif;
  background-color: #ffffff;
  transition: border-color 0.3s ease;
}

.form-input:focus {
  outline: none;
  border-color: #26a69a;
  box-shadow: 0 0 5px rgba(38, 166, 154, 0.2);
}

.form-input.prefilled {
  background-color: #fff8e1;
  color: #ff8f00;
  font-weight: 500;
  border-color: #ffcc02;
}

.form-input::placeholder {
  color: #999;
  font-style: italic;
}

.form-actions {
  display: flex;
  justify-content: center;
  gap: 15px;
  margin-top: 20px;
}

.reserve-btn {
  background: linear-gradient(90deg, #26a69a, #4dd0e1);
  color: white;
}

.reserve-btn:hover {
  background: linear-gradient(90deg, #4dd0e1, #26a69a);
  transform: translateY(-1px);
}

.cancel-btn {
  background-color: #f5f5f5;
  color: #666;
  border: 2px solid #e0e0e0;
}

.cancel-btn:hover {
  background-color: #eeeeee;
  transform: translateY(-1px);
}

.release-btn {
  background-color: #e57373;
  color: white;
}

.release-btn:hover {
  background-color: #ef5350;
  transform: translateY(-1px);
}

/* Responsive Design */
@media (max-width: 600px) {
  .container {
    padding: 10px;
  }
  .dash-container {
    max-width: 100%;
    padding: 0;
  }
  .card {
    padding: 10px;
  }
  .card h3 {
    font-size: 14px;
  }
  .history-table-wrapper, .lots-container {
    overflow-x: auto; /* Ensure horizontal scroll on small screens */
  }
  .history-table, .lots-table {
    min-width: 100%; /* Allow full width with scroll */
    display: block; /* Stack table rows */
  }
  .history-table thead, .lots-table thead {
    display: none; /* Hide headers on small screens */
  }
  .history-table tbody, .lots-table tbody,
  .history-table tr, .lots-table tr {
    display: block;
    width: 100%;
  }
  .history-table td, .lots-table td {
    display: flex;
    justify-content: space-between;
    padding: 6px;
    font-size: 12px;
    border-bottom: 1px solid #ddd;
  }
  .history-table td:last-child, .lots-table td:last-child {
    border-bottom: none;
  }
  .history-table td:before, .lots-table td:before {
    content: attr(data-label);
    font-weight: 500;
    color: #26a69a;
    margin-right: 10px;
    white-space: nowrap;
  }
  .history-table td[data-label="ID"]:before { content: "ID: "; }
  .history-table td[data-label="Location"]:before { content: "Location: "; }
  .history-table td[data-label="Vehicle No"]:before { content: "Vehicle No: "; }
  .history-table td[data-label="Timestamp"]:before { content: "Timestamp: "; }
  .history-table td[data-label="Action"]:before { content: "Action: "; }
  .lots-table td[data-label="ID"]:before { content: "ID: "; }
  .lots-table td[data-label="Address"]:before { content: "Address: "; }
  .lots-table td[data-label="Availability"]:before { content: "Availability: "; }
  .lots-table td[data-label="Action"]:before { content: "Action: "; }
  .action-btn {
    width: 100%;
    margin-top: 5px;
  }
  .search-container {
    flex-direction: column;
    align-items: stretch;
  }
  .search-input .input-field {
    width: 100%;
    margin-bottom: 10px;
  }
  .search-btn {
    width: 100%;
  }
  .form-group {
    flex-direction: column;
    align-items: flex-start;
  }
  .form-group label {
    width: 100%;
    margin-bottom: 5px;
    margin-right: 0;
    text-align: left;
    font-size: 12px;
  }
  .form-input {
    width: 100%;
    font-size: 12px;
  }
  .form-actions {
    flex-direction: column;
  }
  .action-btn {
    width: 100%;
    margin-bottom: 10px;
  }
  .action-btn:last-child {
    margin-bottom: 0;
  }
  .modal-content {
    width: 85%;
    padding: 15px;
  }
}